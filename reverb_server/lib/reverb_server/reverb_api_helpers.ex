defmodule ReverbApp.ReverbAPIHelpers do
  alias ReverbApp.ReverbAPI, as: API
  require Logger

  # def get_all_categories_with(token) do
  #   case API.get_categories_flat()
  #     {:error, _} ->
  #       Logger.error("Request for categories with #{token} failed.")
  #     {:ok, categories} -> Logger.info("Do stuff here!")
  # end
end
